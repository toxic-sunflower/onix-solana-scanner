services:
  app_blue:
    build:
      context: .
      dockerfile: src/Onix.Scanner.Api/Dockerfile
    container_name: onix_app_blue
    network_mode: host
    env_file: .env
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://0.0.0.0:5000
      - Telegram__BotToken=${TELEGRAM_BOT_TOKEN}
      - Telegram__BotUsername=${TELEGRAM_BOT_USERNAME}
      - Telegram__OpenId__ClientId=${TELEGRAM_OAUTH_CLIENT_ID}
      - Telegram__OpenId__ClientSecret=${TELEGRAM_OAUTH_CLIENT_SECRET}
      - Telegram__OpenId__RedirectUri=${TELEGRAM_OAUTH_REDIRECT_URI}
      - Telegram__WebhookSecret=${TELEGRAM_WEBHOOK_SECRET}
      - Encryption__Key=${ENCRYPTION_KEY}
      - ConnectionStrings__Default=Host=localhost;Database=onix_scanner;Username=onix;Password=${DB_PASSWORD}
      - App__Url=${APP_URL}
    healthcheck:
      test: curl -f http://localhost:5000/api/v1/health || exit 1
      interval: 15s
      retries: 5
      start_period: 60s
    restart: unless-stopped

  app_green:
    build:
      context: .
      dockerfile: src/Onix.Scanner.Api/Dockerfile
    container_name: onix_app_green
    network_mode: host
    env_file: .env
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://0.0.0.0:5001
      - Telegram__BotToken=${TELEGRAM_BOT_TOKEN}
      - Telegram__BotUsername=${TELEGRAM_BOT_USERNAME}
      - Telegram__OpenId__ClientId=${TELEGRAM_OAUTH_CLIENT_ID}
      - Telegram__OpenId__ClientSecret=${TELEGRAM_OAUTH_CLIENT_SECRET}
      - Telegram__OpenId__RedirectUri=${TELEGRAM_OAUTH_REDIRECT_URI}
      - Telegram__WebhookSecret=${TELEGRAM_WEBHOOK_SECRET}
      - Encryption__Key=${ENCRYPTION_KEY}
      - ConnectionStrings__Default=Host=localhost;Database=onix_scanner;Username=onix;Password=${DB_PASSWORD}
      - App__Url=${APP_URL}
    healthcheck:
      test: curl -f http://localhost:5001/api/v1/health || exit 1
      interval: 15s
      retries: 5
      start_period: 60s
    restart: unless-stopped

  # Internal Blazor Server admin panel — single instance (no blue/green, brief
  # restart on deploy is fine for an internal tool). Login has no 2FA, only a
  # rate limit — so it stays bound to 127.0.0.1 (not 0.0.0.0) regardless of
  # how it's reached externally: nginx runs on this same host (network_mode:
  # host), so it can reach 127.0.0.1:5050 directly without the port ever
  # appearing on the public interface. Exposed at /admin on the main domain
  # via nginx (PathBase below + the matching nginx location block — see
  # TODO.md); still reachable via SSH tunnel too if nginx isn't set up yet
  # (`ssh -L 5050:127.0.0.1:5050 app@server`).
  admin:
    build:
      context: .
      dockerfile: src/Onix.Scanner.Admin/Dockerfile
    container_name: onix_admin
    network_mode: host
    env_file: .env
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://127.0.0.1:5050
      - PathBase=/admin
      - Admin__Username=${ADMIN_USERNAME}
      - Admin__Password=${ADMIN_PASSWORD}
      - Encryption__Key=${ENCRYPTION_KEY}
      - ConnectionStrings__Default=Host=localhost;Database=onix_scanner;Username=onix;Password=${DB_PASSWORD}
    healthcheck:
      test: curl -f http://127.0.0.1:5050/admin/login || exit 1
      interval: 15s
      retries: 5
      start_period: 30s
    restart: unless-stopped
