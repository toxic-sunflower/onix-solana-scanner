@inject NavigationManager Nav

@code {
    // Relative (no leading slash) so it resolves against <base href> — with a
    // leading slash this would ignore PathBase and land on the domain root.
    protected override void OnInitialized() => Nav.NavigateTo("login", forceLoad: true);
}
